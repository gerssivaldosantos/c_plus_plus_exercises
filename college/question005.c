#include <stdio.h>;

main(){
    int number;
    printf("Type a interger number, and i will show are sucessor : ");
    scanf("%d",& number);
    printf("the sucessor for the namber is %d \n", number + 1);
    return 0;
}